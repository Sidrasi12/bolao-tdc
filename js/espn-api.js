Bolao.ESPN={async games(week=1,type=2){const u=`https://site.api.espn.com/apis/site/v2/sports/football/nfl/scoreboard?limit=100&dates=${BOLAO_CONFIG.season}&seasontype=${type}&week=${week}`;const r=await fetch(u);if(!r.ok)throw Error('Falha ao consultar jogos');const j=await r.json();return(j.events||[]).map(e=>{const c=e.competitions[0],a=c.competitors.find(x=>x.homeAway==='away'),h=c.competitors.find(x=>x.homeAway==='home');return{id:e.id,date:e.date,status:e.status.type.name,completed:e.status.type.completed,venue:c.venue?.fullName||'Estádio a definir',venueCity:c.venue?.address?.city||'',away:{name:a.team.displayName,abbr:a.team.abbreviation,logo:a.team.logo,score:+a.score},home:{name:h.team.displayName,abbr:h.team.abbreviation,logo:h.team.logo,score:+h.score},playoff:type===3}})},difficulty(g){return Math.abs(g.home.score-g.away.score)>10?'VF':'VD'}};